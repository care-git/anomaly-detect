# requirements.txt

# Python >=3.10 recommended

# Core ML libraries
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.8.0
tensorflow==2.21.0
joblib==1.5.3

# Packet processing
scapy==2.7.0

# Config + logging
pyyaml==6.0.3
tqdm==4.67.3

# Visualisation
matplotlib==3.10.9
seaborn==0.13.2

# GPU acceleration (optional - Linux + CUDA only)
# cuml is NOT pip-installable. Install via conda on NVIDIA GPU machines:
#   conda install -c rapidsai -c conda-forge -c nvidia cuml
# Then set use_gpu: true in config/config.yml.